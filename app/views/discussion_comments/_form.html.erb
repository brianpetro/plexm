<%= simple_form_for([@bounty, @discussion_comment]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :comment %>
    <%= f.input :bounty_id %>
    <%= f.input :discussion_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
