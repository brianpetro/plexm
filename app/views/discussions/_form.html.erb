<%= simple_form_for(@discussion) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :discussion %>
    <%= f.input :user_id %>
    <%= f.input :bounty_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
